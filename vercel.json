{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"installCommand": "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --profile minimal --default-toolchain stable --no-modify-path && export PATH=\"$HOME/.cargo/bin:$PATH\" && pnpm install",
	"buildCommand": "pnpm run build"
}
